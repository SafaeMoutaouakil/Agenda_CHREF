/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Index_ from './src/screens/Index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Index_);
